spring:
  application:
    name: ArcView WoT Bridge

graphql:
  packages:
    - "ai.ancf.lmos.arc.bridge"
    - "ai.ancf.lmos.sdk.model"
    - "ai.ancf.lmos.sdk.agents"
  playground:
    enabled: true

server:
  port: 8300

wot:
  servient:
    http:
      server:
        enabled: false
    websocket:
      server:
        enabled: false